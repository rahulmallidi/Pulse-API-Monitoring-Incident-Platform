/** @type {import('next').NextConfig} */
const nextConfig = {
  // Default Next output works on Vercel. Use `output: "standalone"` only for Docker images.
  reactStrictMode: true
};

export default nextConfig;
