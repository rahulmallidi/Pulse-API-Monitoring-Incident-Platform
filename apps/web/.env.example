# Copy values into Vercel → Project → Settings → Environment Variables
# Root Directory must be: apps/web

# Public API origin (Railway / Fly / Render / VPS) — no trailing slash
NEXT_PUBLIC_API_BASE_URL=https://your-pulse-api.example.com
